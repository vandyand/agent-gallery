# OpenRouter — single key routes to Claude Haiku (artists), and a diverse
# vision judge panel (gemini-flash, gpt-4o-mini). Get one at openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-...

# Optional: hard per-run USD ceiling for the offline engine (safety, SPEC §8)
GALLERY_MAX_USD=2.00
